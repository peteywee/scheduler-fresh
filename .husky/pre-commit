# Run secret scan (non-fatal if gitleaks missing) + lint-staged
npm run precommit:secrets || echo "⚠️  precommit:secrets skipped/failed"
# Prefer npx lint-staged
npx --yes lint-staged || echo "⚠️ lint-staged failed or not available"
