{
  "name": "scheduler-fresh-functions",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "date-fns": "^4.1.0",
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^4.9.0",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "eslint": "^9.36.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.9.2"
  }
}
