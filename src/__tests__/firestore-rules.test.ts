import { describe, it } from "vitest";

describe("Firestore Security Rules (legacy placeholder)", () => {
  it("moved to src/test/firestore.rules.test.ts", () => {
    // Tests live in src/test/firestore.rules.test.ts.
  });
});
